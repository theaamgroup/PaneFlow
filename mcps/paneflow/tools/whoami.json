{
  "name": "whoami",
  "description": "Resolve your inherited PaneFlow pane, workspace, terminal lifetime, and observed agents. IDs are routing metadata, not credentials.",
  "annotations": {
    "readOnlyHint": true,
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false
  },
  "inputSchema": {
    "type": "object",
    "properties": {},
    "additionalProperties": false
  }
}
