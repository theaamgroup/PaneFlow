# Plan of record: everything after stage 2c

Written 2026-08-25, immediately after 2c landed at `0e720e2`.

This file is the **execution plan**. The live task list lives in the harness
(twelve tasks: IDs 1-7 and 10-15; 8 and 9 were the dropped rename); this
document holds the decisions, the ordering, the machinery, and the things a task
description cannot carry. Read
`docs/fork/STATE.md` first for where the tree stands and the method rules the
project has already paid for.

## Where the tree is right now

Verified at `0e720e2`, quoting the actual run:

```
cargo fmt --check                        exit 0
cargo build                              exit 0
cargo clippy --workspace --all-targets   exit 0, warning count 1 (block v0.1.6)
cargo test --workspace                   1725 passed, 0 failed, 2 ignored
./target/debug/paneflow --version        paneflow 0.8.2
./scripts/linux-census.sh                STAGE 2c ZERO-CONDITION: 0
                                         negative control: cfg(unix) 152, cfg(macos) 77
```

Branch `mac-only-fork`, 58 commits ahead of the fork point. Working tree clean.
No worktrees, no leftover batch branches.

## Decisions locked (2026-08-25, from the user)

| # | Question | Decision |
|---|---|---|
| 1 | `windows_*_material`, `TerminalBackendConfig::Ghostty`, `mica`/`acrylic` | **Drop from the published schema, the Rust struct and the docs. Keep the loader tolerant** so an existing `paneflow.json` carrying them still loads and they have no effect. `"backend":"ghostty"` falls back to alacritty rather than failing to parse. |
| 2 | The 2d rename to PanesCLI | **DROPPED.** The product stays PaneFlow. See "The rename, and why it is not happening" below. |
| 3 | Stage 3 release | **Rewrite both workflows now.** Cut `run_tests.yml` to macOS, reduce `release.yml` to one signed `aarch64-apple-darwin` lane, strip GPG/Azure/PostHog. The `APPLE_*` secrets and the minisign keypair are the user's to create afterwards. |
| 4 | The queued small items | **All four**: Cmd+Tab, the libproc RSS/CPU probes, the Mach-O size re-baseline, and the restored test coverage. |
| 5 | Self-update, given `checker.rs:28` still points at upstream | **Disable the feed.** `theaamgroup/panescli` is private, so its release assets are not anonymously downloadable, and there is no other host yet. Keep minisign verification, `verified_download`, the DMG installer and `UpdateError` intact - re-enabling must be a one-line feed URL. |
| 6 | Version identity | **Reset to 0.1.0**, done last, immediately before the first tag. Safe only because the feed is disabled (task 11), so no installed build can see a downgrade. |
| 7 | Telemetry (~1300 lines, permanently keyless) | **Remove the subsystem entirely** - crate, app module, event emitters, config block, consent UI. Do it before the `release.yml` rewrite so stripping `POSTHOG_API_KEY` is dead-reference removal rather than a judgement call. |
| 8 | The GitHub repo is named `theaamgroup/panescli` while the product is PaneFlow | **Rename the repo to `theaamgroup/paneflow`** (GitHub keeps redirects), then sweep every reference so there is one name everywhere. Task 15. |

## The rename, and why it is not happening

The 2d rename was planned, scoped (266 files, 4159 occurrences, 9 crates and
their directories, a config/session migration shim) and then dropped after the
trade was laid out. It delivered no functional value, and it carried the only
runtime failure surface left in the project - config dir, socket path, PATH-shim
basenames, the ai-hook sibling lookup, MCP re-registration - none of which the
1725 tests or the census can see. The two arguments that made it urgent had both
weakened by then: the repo is private, and self-update is disabled, so there is
no public distribution where the product name is doing any work.

What genuinely had to change was **upstream's identity**, not the product name:
`CFBundleIdentifier = io.github.arthurdev44.paneflow`, `authors = ["Arthur"]`,
and the `repository`/`homepage` pointing at upstream. macOS keys TCC grants,
LaunchServices and the preferences domain off the bundle id, so signing under
your own Apple Team ID while claiming someone else's reverse-DNS is wrong and
collides with an installed upstream build. That is task 12 - four files.

GPL-3.0-or-later attribution to upstream is satisfied by `LICENSE` and the
README line, not by the bundle id. Both stay.

If PaneFlow is ever distributed publicly under its own identity, revisit the
rename then, against a tree that is by that point much smaller.

The name `PanesCLI` survives in ten files as leftovers of that plan - two doc
titles, two naming tables, a stage row, and six links to the repo, which is
itself still named `theaamgroup/panescli`. Task 15 resolves both halves: the
repo is renamed to `paneflow` so the URLs stay truthful, and the product-name
prose is swept. None of it is code; every hit is `.md` or `.yml`.

## Order, and why

```
  4 size re-baseline ──> 2 run_tests.yml cut ──┐
 13 remove telemetry ──┐                       │
 12 fix identity     ──┴─> 3 release.yml ──────┤
  1 config-schema                              ├──> 14 version reset to 0.1.0
 11 disable update feed                        │      (last, immediately before tagging)
  5 libproc probes                             │
  6 Cmd+Tab                                    │
  7 test coverage ─────────────────────────────┘
 10 residue backlog: no dedicated pass, folded into whichever task touches the file
```

Three real dependencies, everything else parallel:

- **4 before 2** - `run_tests.yml` re-checks the embedded-size budget, so it
  needs the honest Mach-O number before its step can be written.
- **13 and 12 before 3** - with telemetry gone, stripping `POSTHOG_API_KEY` from
  the release workflow is dead-reference removal; with the bundle id fixed, the
  signing steps are written against the real identity.
- **Everything before 14** - a version bump invalidates every "verified at
  0.8.2" line in the docs, so doing it early just means editing them twice.

## What needs the user, and nothing else does

Only the **secrets**, and only after task 3.

- The Apple values the signing and notarization steps consume: `APPLE_ID`,
  `APPLE_TEAM_ID`, `APPLE_APP_SPECIFIC_PASSWORD`, the developer certificate
  `.p12` and its password. Note the current file references both
  `APPLE_DEVELOPER_CERT_PASSWORD` and a truncated-looking
  `APPLE_DEVELOPER_CERT_P` - check whether that is a typo or a split value when
  you set them up, because a mismatch fails late in a release run.
- A fresh `MINISIGN_SECRET_KEY`, with its public half embedded at build time via
  `PANEFLOW_MINISIGN_PUBKEY` (`src-app/src/update/signature.rs:35-36` documents a
  `_NEXT` slot for rotation).

Task 3 leaves the list in `docs/release-runbook.md`. **Never create `GPG_*`,
`AZURE_TRUSTED_SIGNING_*` or `POSTHOG_API_KEY` in this org** - see the leak
register in `docs/fork/2026-08-25-mac-only-fork-design.md` for what each fed
upstream.

Note that self-update is disabled (task 11), so a signed DMG is currently
something you distribute by hand. Re-enabling it later means picking a
distribution host and setting one URL.

## The execution model that worked, and should be reused

Stage 2c ran twelve delegated batches with zero re-briefs. The thing that made
it work is worth stating plainly, because the old advice in STATE.md said the
opposite and was wrong:

> **Fan-out works when the worker does not have to discover anything.**

The briefs stopped saying "find the Linux code" and started carrying the exact
site list - `file:line`, the cfg expression as written, the action, the
reduction. That list came from a read-only inventory phase that ran *before* any
edit. A batch that must find its own targets in a codebase this interleaved will
eventually delete a `cfg(unix)` arm.

### Mechanics

```bash
GROK="$HOME/.grok/bin/grok"            # never bare `grok` - that is the PATH shim
git worktree add /Users/dayers/Github/pf-X -b <branch> mac-only-fork
cp -c -R target /Users/dayers/Github/pf-X/target   # APFS clone: ~27s for three, ~0 bytes
"$GROK" --prompt-file brief-X.md --cwd /Users/dayers/Github/pf-X \
        --max-turns 100 --json-schema "$(cat schema-exec.json)" \
        > out-X.json 2>err-X.txt
```

- Worktrees are reusable between waves: `git -C <wt> reset --hard` then
  `git -C <wt> checkout -B <branch> mac-only-fork`.
- Three concurrent batches was the working cap on this machine.
- **Disjoint file sets are what make concurrency safe.** Two batches editing the
  same file collide at `git apply` time even when their edits are six lines
  apart. Check the allowlists against each other before launching.
- Agents never touch git. They leave edits unstaged; the orchestrator collects
  `git -C <wt> diff`, applies it to the main worktree, re-runs all five gates
  **itself**, and writes the commit.

### Every brief opens with this, verbatim

> `#[cfg(unix)]` is TRUE on macOS. macOS IS a unix. Never remove one.
> `unix` is not `linux`. Only `all(unix, not(target_os = "macos"))` is Linux.

152 `cfg(unix)` and 77 `cfg(macos)` sites survive deliberately. That warning is
why no batch in 2c got it wrong.

### Every brief carries this gate contract

```bash
cargo fmt                                # FIRST, not last
cargo build                              # exit 0
cargo clippy --workspace --all-targets   # exit 0 AND warning count exactly 1
cargo test --workspace                   # compare NAMES, never the count
cargo fmt --check                        # exit 0
./scripts/linux-census.sh                # report the totals AND the negative control
```

`cargo fmt` runs first because rustfmt collapses a surviving block onto one line
and makes `unused_braces` fire only on the reformatted source. **Clippy exits 0
with warnings**, so "clippy exit 0" is not evidence - the warning COUNT is.
There is exactly one pre-existing warning (`block v0.1.6`) and it is not ours.

### Integration protocol

1. Read the agent's structured result. Check `num_turns` - a `1` on an
   open-ended task means `--json-schema` suppressed the tool loop and the answer
   is fabricated. (It is legitimate when the whole task is inline in the prompt,
   as with the blind verifier.)
2. Read the actual diff. Agent "green" claims are never the evidence; in stage
   2b a batch reported green from a clippy run that predated its own final edits.
3. `git apply --check` then `git apply` into the main worktree.
4. Run all five gates yourself, plus the census.
5. **Diff the sorted test-name list against the previous commit's.** Every
   removal and every rename gets named in the commit message with the
   arithmetic. This is how 65 removals and 4 renames stayed auditable.
6. `cargo fmt --check` immediately before `git commit` - the release pipeline
   runs it inside every build leg, and a tag push on a dirty commit burns a
   ~25 min run and cannot be salvaged without re-tagging.

## Verification contract for every task

Non-negotiable, before and after:

| Gate | Expected |
|---|---|
| `cargo fmt --check` | exit 0 |
| `cargo build` | exit 0 |
| `cargo clippy --workspace --all-targets` | exit 0, **warning count 1** |
| `cargo test --workspace` | 0 failed; count changes accounted for BY NAME |
| `./target/debug/paneflow --version` | runs |
| `./scripts/linux-census.sh` | ZERO-CONDITION **0**; negative control **152 / 77** |

The negative control is not decoration. A census printing 0 because its regex
broke looks identical to one printing 0 because the work is done, and that has
happened twice here - once when the regex matched the `update/linux/` PATH, once
when it could not see `!cfg!(target_os = "macos")`.

**A census at 0 is not a finished platform removal.** After 2c hit zero, an
adversarial audit found five more classes the cfg scan structurally cannot see:
ungated identifiers, an enum the collapse missed, user-visible copy, dependency
-graph residue, and YAML. Four were fixed; the fifth is task 2. Run that audit
again at the end of the rename - without `--json-schema`, which suppressed the
tool loop four times in this project's history.

## Task list

| # | Task | Blocked by |
|---|---|---|
| 1 | Config-schema pass: drop the deferred Windows/Ghostty surface | - |
| 2 | Cut `run_tests.yml` to the macOS legs and fix the aggregator | 4 |
| 3 | Reduce `release.yml` to a single signed aarch64-apple-darwin lane | 12, 13 |
| 4 | Re-baseline `EMBED_SIZE_LIMIT_BYTES` on Mach-O | - |
| 5 | Implement the libproc RSS and CPU-time probes | - |
| 6 | Verify and fix the Cmd+Tab next-workspace binding | - |
| 7 | Restore the test coverage thinned by the platform removal | - |
| 10 | Low-priority residue backlog (folded into other passes) | - |
| 11 | Disable the self-update feed (it currently points at upstream) | - |
| 12 | Replace upstream's identity: bundle id, authors, repository, homepage | - |
| 13 | Remove the telemetry subsystem entirely | - |
| 14 | Reset the version to 0.1.0 | 1-7, 11, 12, 13 |
| 15 | Make the naming consistent: PaneFlow everywhere, retire PanesCLI | repo rename on GitHub |

Tasks 8 and 9 were the PanesCLI rename and its verification; both are deleted.

**Task 15 has a human precondition.** The GitHub repo rename is the user's to do
(`gh repo rename paneflow -R theaamgroup/panescli`). The task verifies it landed
with `gh repo view` before rewriting a single URL - pointing six files at a repo
name that does not exist is worse than the inconsistency.

Full detail - exact sites, traps, per-task verification - lives in the task
descriptions, not here.

## Method rules that apply to all of it

From `docs/fork/STATE.md`, repeated because they are the ones most likely to be
skipped under time pressure:

1. **Never report a finding you have not observed.** Four claims in this project
   have been falsified by executing something. Task 6 exists entirely because a
   keybinding claim needs observing rather than reasoning about.
2. **Add a control.** If the control reproduces the positive result, the
   experiment is broken. The blind `cfg!()` verifier in 2c carried a planted
   CHANGED hunk for exactly this reason, and it caught an over-claim of mine.
3. **Do not pipe a command whose exit code matters.** `cargo test | tail`
   reports `tail`'s status. Redirect as `cmd > file 2>&1`, never
   `cmd 2>&1 > file`.
4. **A green `cargo build` is not a green tree.**
5. **A detector only measures the shape it was written for.** The census
   measures cfg predicates. Ungated code, enum variants, `Cargo.toml` tables,
   embedded assets, workflow files and user-facing strings each need their own
   sweep.
6. **Inventory line numbers go stale the moment you commit.** Re-derive
   `file:line` from the live tree before writing a brief, and tell the agent to
   stop and report if a line does not say what the brief claims.
