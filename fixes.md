# Fixes for last several PRs

Generated by /deep-review on 2026-08-30 at commit 738430c. Run directory: ~/.agents/runs/deep-review/paneflow/20260830-1551.

## How to use this file
Each task is self-contained. Work them in order. A task is finished when its **Done when** line holds. Hand-off: `/autopilot --track=feature Implement every PENDING task in /Users/dayers/Github/paneflow/fixes.md`. Keep `--track=feature`; without it autopilot classifies "every task" as grind and cannot finish, because it never edits this file. This hand-off is for a file written with `--no-auto-fix`; after a default run the tasks are already DONE as uncommitted edits in the checkout, which autopilot's worktree will not contain, so commit and push them before handing over anything still PENDING. Work only tasks whose **State** is `PENDING`; never work a `BLOCKED` task, and never change a test's expected values, delete a test, or widen a timeout to satisfy a Done when line. Mark a task by editing its **State** line to `DONE`, `BLOCKED <reason>`, or `SKIPPED <reason>`. Remaining work: `grep -c '^\*\*State:\*\* PENDING' /Users/dayers/Github/paneflow/fixes.md`.

## Context
- Target: last several PRs (type: code)
- Perspectives run: Correctness (general-purpose), Security (general-purpose), Design and maintainability (general-purpose), Verifiability (general-purpose), Performance and resilience (general-purpose)
- Findings filtered out as intentional: 2 (see "Ignored" at the bottom)
- Check command: cargo test --workspace
- Reviewed at: 738430c on fix/all-open-issues-20260830

## Tasks

### 1. [S2] OpenCode plugin merge no-ops on non-array plugin
**State:** PENDING
**Severity:** S2 (major)
**Where:** `crates/paneflow-shim/src/hooks/opencode.rs:180` in `merge_opencode_plugin_entry`
```
    let plugins = root
        .entry("plugin")
        .or_insert_with(|| serde_json::json!([]));
    let Some(plugins) = plugins.as_array_mut() else {
        return;
    };
    plugins.retain(|entry| !is_paneflow_plugin_entry(entry));
    plugins.push(serde_json::Value::String(plugin_path.to_owned()));
```
**What is wrong:** If `plugin` already exists as a string or object, `as_array_mut` fails and the function returns without recording the plugin and without an error. `install_at` still writes the `.ts` file and returns `Ok`.
**Why it matters:** OpenCode never loads the PaneFlow status plugin, but the shim reports a successful hook install. Uninstall uses the same guard and cannot remove a non-array entry.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Change `merge_opencode_plugin_entry` to return `std::io::Result<()>` and, when `plugin` exists and is not an array, return `Err` with a message that names the existing type. Propagate that error from `install_at` so the install is not reported as success. Apply the same error on remove.
**Done when:** `cargo test -p paneflow-shim -- merge_opencode_plugin_entry` includes a test where `"plugin": "other"` or `"plugin": {}` makes `install_at` return Err and the test fails if that fixture still returns Ok.
**Found by:** Design and maintainability
**Lead check:** quote present at `crates/paneflow-shim/src/hooks/opencode.rs:180`; claim confirmed

### 2. [S2] Files sidebar refresh blocks the GPUI thread
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/app/files_sidebar/watch.rs:185` in `PaneFlowApp::refresh_files_dirs`
```
        for dir in files_tree::coalesce_by_prefix(dirs) {
            // AC4: only re-read directories we've already cached (expanded).
            if let std::collections::hash_map::Entry::Occupied(mut e) =
                self.files_tree.children.entry(dir.clone())
            {
                e.insert(files_tree::read_dir_sorted(&root, &dir));
                changed = true;
            }
        }
```
**What is wrong:** `refresh_files_dirs` calls `read_dir_sorted` (up to 2000 `read_dir` plus `metadata` syscalls per expanded dir) on the thread that invoked it. Bootstrap drains watcher events via `cx.update`, so that thread is the GPUI main thread. Open-path hydration already uses `smol::unblock`; this refresh path does not.
**Why it matters:** A git checkout, save storm, or notify Rescan freezes input and painting, and the 50 ms IPC/automation tick waits behind it.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Re-read cached directories on a background executor keyed by `files_hydrate_generation`, and apply the listings on the main thread only if the sidebar root and generation still match.
**Done when:** `rg -n 'read_dir_sorted' src-app/src/app/files_sidebar/watch.rs` shows no call from `refresh_files_dirs` on the GPUI path; a unit or comment-locked test asserts refresh apply uses the same generation guard as hydration.
**Found by:** Performance and resilience
**Lead check:** quote present at `src-app/src/app/files_sidebar/watch.rs:185`; claim confirmed

### 3. [S2] One failed dir watch drops the whole Files watcher
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/app/files_sidebar/watch.rs:223` in `build_files_watcher`
```
        if let Err(e) = watcher.watch(&dir, notify::RecursiveMode::NonRecursive) {
            log::warn!(
                "files watcher: failed to watch {} ({e}); falling back to on-expand reads",
                dir.display()
            );
            return None;
        }
```
**What is wrong:** The loop already watches the root, then `return None` on the first expanded-dir `watch` error drops that local watcher, including the successful root watch. `watch_files_dir` in the same file logs and keeps the rest of the watcher.
**Why it matters:** One unreadable or vanished expanded directory disables live refresh for the whole Files sidebar until it is closed and reopened.
**How to observe it:** Static finding; not executed.
**Recommended fix:** In `build_files_watcher`, log and skip a failed non-root `watch`, and still return `Some((watcher, rx))` when the root watch succeeded. Return `None` only when `recommended_watcher` itself cannot be created or the root watch fails.
**Done when:** `cargo test -p paneflow-app files_watcher_keeps_root_watch_when_one_expanded_dir_fails` fails if a non-root `watch` error still makes `build_files_watcher` return None.
**Found by:** Design and maintainability, Correctness
**Lead check:** quote present at `src-app/src/app/files_sidebar/watch.rs:223`; claim confirmed

### 4. [S2] In-flight persist drops a pending config reload
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/app/ipc_handler.rs:1666` in `PaneFlowApp::process_config_changes`
```
        let new_config = self
            .pending_config
            .lock()
            .unwrap_or_else(|e| e.into_inner())
            .take();
        if let Some((config, incoming_gen)) = new_config {
```
**What is wrong:** `take()` removes the ConfigWatcher deposit, then `should_apply_watcher_config` can refuse it because `config_persist_in_flight > 0`. The skipped payload is not written back. A strictly older `incoming_gen` should stay discarded; an in-flight persist skip must defer.
**Why it matters:** A hand-edit that lands while a settings persist is in flight never reaches `cached_config`. The UI stays on the in-memory snapshot until another later file event.
**How to observe it:** Static finding; not executed.
**Recommended fix:** If the deposit is skipped only because a persist is in flight, put `(config, incoming_gen)` back into `pending_config` when the slot is still empty (keep a newer deposit if the watcher wrote again). Apply it on the next tick after `in_flight` returns to 0.
**Done when:** `cargo test -p paneflow-app process_config_changes` includes a test that a deposit taken while `in_flight > 0` is still applied after the persist guard drops, and a strictly older `incoming_gen` is still discarded.
**Found by:** Design and maintainability
**Lead check:** quote present at `src-app/src/app/ipc_handler.rs:1666`; claim confirmed

### 5. [S2] Worktree live-cwd check calls cwd_now on the UI thread
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/app/workspace_ops/mod.rs:1173` in `PaneFlowApp::live_workspace_uses_worktree`
```
                                }) || terminal
                                    .terminal
                                    .cwd_now()
                                    .is_some_and(|cwd| path_is_within_worktree(&cwd, worktree_path))
```
**What is wrong:** For every terminal in every workspace, this GPUI-thread gate issues `proc_pidinfo` via `cwd_now` and, on mismatch, `path_is_within_worktree` may `canonicalize` both paths. Callers include IPC `managed_worktree_conflicts` and retirement.
**Why it matters:** `workspace.up` / split / close with many panes does O(workspaces × panes × terminals) blocking syscalls on the 50 ms automation tick.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Use already-cached `current_cwd` on the main thread. Sample `cwd_now` / canonicalize off-thread, or only for terminals whose cached cwd is missing. Compare against a pre-canonicalized worktree path once.
**Done when:** `rg -n 'cwd_now' src-app/src/app/workspace_ops/mod.rs` shows no `cwd_now` call inside `live_workspace_uses_worktree`; a test of that helper uses `current_cwd` only.
**Found by:** Performance and resilience
**Lead check:** quote present at `src-app/src/app/workspace_ops/mod.rs:1173`; claim confirmed

### 6. [S2] Flow ready HashSet drops a repeated sentinel
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/cli/flow_cmd.rs:1042` in `new_text_since_baseline`
```
    let old_lines: HashSet<&str> = baseline.lines().collect();
    current
        .lines()
        .filter(|line| !old_lines.contains(line))
        .collect::<Vec<_>>()
        .join("\n")
```
**What is wrong:** When the 500-line `surface.read` window is no longer a prefix of the baseline, “new” text is a set-difference on exact line strings. A ready line whose text also appeared in the baseline is dropped even if it is printed again after the window slides.
**Why it matters:** A step such as `ready = { pattern = "tests passed" }` can sit until `timeout_secs` after a long turn that reprints a sentinel already present in earlier scrollback, then fail the flow.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Replace `flow_cmd.rs` `new_text_since_baseline` with an ordered suffix/sequence diff (keep the `strip_prefix` fast path; drop the `HashSet` membership fallback).
**Done when:** `cargo test -p paneflow-app --test '*' flow_ready_matches_repeated_sentinel_after_window_slides` (or the module test of that name) marks a polling unit Ready when the post-feed window is not a prefix of the fire_feed baseline but contains the ready pattern as a later identical line.
**Found by:** Correctness
**Lead check:** quote present at `src-app/src/cli/flow_cmd.rs:1042`; claim confirmed

### 7. [S2] wait treats unreachable as a skipped poll
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/cli/wait_cmd.rs:271` in `poll_matches_since`
```
    match read_matches_since(client, id, re, baseline) {
        Err(e) if is_transient_read_error(&e.message) => Ok(PaneState::NoMatch),
        result => result,
    }
```
**What is wrong:** After the baseline read, `is_transient_read_error` treats `"unreachable"` and `-32002` as `NoMatch`. `read_snapshot` documents a down instance as fatal, then `poll_matches_since` converts that same error into a skipped poll. `flow_cmd.rs` `is_transient_read_error` does not include `"unreachable"`.
**Why it matters:** A quit or crashed PaneFlow makes `paneflow wait` sit until `--timeout` (default 300s) instead of exiting with “is Paneflow running?”. Possibly intentional: "transient IPC backpressure or transport failures can safely skip one poll" (`src-app/src/cli/wait_cmd.rs:268-270`).
**How to observe it:** Static finding; not executed.
**Recommended fix:** Remove `"unreachable"` from `wait_cmd.rs` `is_transient_read_error`. Keep busy/timeout/`-32000` as skipped polls. Share one classifier with `flow_cmd.rs` in a later change if both still duplicate the helper.
**Done when:** `cargo test -p paneflow-app wait_unreachable_after_baseline_is_hard_error` fails if `poll_matches_since` still maps an “unreachable” error to `PaneState::NoMatch`.
**Found by:** Design and maintainability
**Lead check:** quote present at `src-app/src/cli/wait_cmd.rs:271`; claim confirmed

### 8. [S2] wait HashSet drops a repeated sentinel
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/cli/wait_cmd.rs:300` in `new_text_since_baseline`
```
    let old_lines: HashSet<&str> = baseline.lines().collect();
    current
        .lines()
        .filter(|line| !old_lines.contains(line))
        .collect::<Vec<_>>()
        .join("\n")
```
**What is wrong:** When the 500-line `surface.read` window is no longer a prefix of the baseline (window slide or in-place redraw), “new” text is a set-difference on exact line strings. A sentinel that already occurred in the original window is dropped even if it is printed again.
**Why it matters:** `paneflow wait --pattern` times out on a re-run in the same pane (for example a second `Build DONE`) once history has moved off the original prefix, even though the new sentinel is on screen.
**How to observe it:** Static finding; not executed.
**Recommended fix:** In `wait_cmd.rs` `new_text_since_baseline`, diff by ordered suffix/sequence, not by `HashSet` membership of whole lines.
**Done when:** `cargo test -p paneflow-app wait_matches_repeated_sentinel_after_window_slides` succeeds when the baseline contains `DONE` and a later 500-line window no longer starts with that baseline but ends with a new `DONE` line.
**Found by:** Correctness
**Lead check:** quote present at `src-app/src/cli/wait_cmd.rs:300`; claim confirmed

### 9. [S2] wait --idle exits success when the pane is gone
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/cli/wait_cmd.rs:445` in `wait_idle`
```
    let baseline = read_snapshot(client, id)?;

    // Ctrl-C is a clean stop; dropping the socket frees the server-side
    // subscription on its next write (RAII), so nothing leaks (US-007 AC4).
    let _ = ctrlc::set_handler(|| std::process::exit(130));

    let params = json!({ "surfaces": [id], "types": ["surface_changed"] });
    let mut since_change = Instant::now();
```
**What is wrong:** A gone pane yields `Ok(None)` and is not treated as failure. The stream then waits for `surface_changed`; a closed pane emits none, so the first `--for` window of ticks is classified Idle. `wait_idle_poll` already errors on `Ok(None)` with `target pane closed before idle wait started`.
**Why it matters:** `paneflow wait --idle` exits 0 after about `--for` ms when the target pane is already gone or is closed during the wait, so a following `send`/`read` runs against a dead surface.
**How to observe it:** Static finding; not executed.
**Recommended fix:** In `wait_idle`, fail when `baseline` is `None` with the same runtime error `wait_idle_poll` uses. On each Tick, if a re-check of `read_snapshot` returns `None`, return that same error instead of `IdleOutcome::Idle`.
**Done when:** `cargo test -p paneflow-app wait_idle_fails_when_target_pane_gone` returns a runtime error for a first `surface.read` of `-32602` / not found, matching `wait_fails_fast_when_target_pane_gone`.
**Found by:** Correctness
**Lead check:** quote present at `src-app/src/cli/wait_cmd.rs:445`; claim confirmed

### 10. [S2] Diff working-tree read has no byte cap
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/diff/git.rs:543` in `load_working_text`
```
        Ok(_) => match std::fs::read(&path) {
            Ok(bytes) => classify(bytes),
            Err(e) if e.kind() == std::io::ErrorKind::NotFound => (String::new(), false),
            Err(e) => {
                log::warn!("git: failed to read working-tree file {rel_path}: {e}");
                (String::new(), true)
```
**What is wrong:** Working-tree bytes are read with unbounded `std::fs::read`. `is_too_large` only stats first and treats a metadata miss as not-too-large. The post-load 512 KiB stub runs after the full buffer is already in RAM. The fingerprint path already caps with `.take(MAX_FILE_BYTES + 1)`.
**Why it matters:** A TOCTOU grow, a metadata miss, or a huge file that slips the pre-check can allocate unbounded memory during a background diff and OOM the process.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Open the file and `take(MAX_FILE_BYTES + 1)` (same as `hash_untracked_inputs`); if the cap is exceeded, return the binary/stub path without retaining the buffer.
**Done when:** `cargo test -p paneflow-app load_working_text_caps_bytes` fails if `load_working_text` retains more than `MAX_FILE_BYTES + 1` bytes from disk when `metadata` fails or the file is larger than the cap.
**Found by:** Performance and resilience
**Lead check:** quote present at `src-app/src/diff/git.rs:543`; claim confirmed

### 11. [S2] Review loads each file with a separate git show
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/diff/git.rs:853` in `compute_diff_against`
```
        let (base_text, base_bin) = match change {
            FileChange::Added => (String::new(), false),
            _ => load_base_text(worktree_dir, base, base_lookup),
        };
        let (new_text, new_bin) = match change {
            FileChange::Deleted => (String::new(), false),
            _ => load_working_text(worktree_dir, &path),
        };
```
**What is wrong:** Each of up to `MAX_FILE_COUNT` (200) changed files runs a separate `git show` via `load_base_text` (`run_git` deadline 30 s). There is no batch (`git cat-file --batch`) and no overall wall-clock cap, so worst-case work is 200 sequential subprocesses.
**Why it matters:** Opening Review on a large changeset occupies a `smol::unblock` worker for many seconds; a slow git helper can pin that worker for minutes (200 × 30 s).
**How to observe it:** Static finding; not executed.
**Recommended fix:** Load blob text with one `git cat-file --batch` for the whole set, and add a single overall deadline covering the column build, not only per-process.
**Done when:** `cargo test -p paneflow-app compute_diff_against_batches_git_show` fails if one worktree diff of many files still issues one `git show` per file.
**Found by:** Performance and resilience
**Lead check:** quote present at `src-app/src/diff/git.rs:853`; claim confirmed

### 12. [S2] First Cmd-hover canonicalizes every path prefix
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/terminal/element/hyperlink.rs:298` in `resolve_path`
```
        let cwd = cwd?;
        cwd.join(path_str)
    };
    candidate.canonicalize().ok()
}
```
**What is wrong:** Cmd/Ctrl+hover runs `detect_file_paths_on_line_mapped` and `detect_code_paths_on_line_mapped` on the UI thread. Each unique prefix from `candidate_start_positions` calls `Path::canonicalize`. A line cache avoids repeats, not the first hover of a new line.
**Why it matters:** First modifier-hover over a long compiler/log line issues tens to hundreds of blocking filesystem walks and stalls the render thread.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Probe only the rightmost path-like token (stop at the nearest left boundary). Keep the per-line cache.
**Done when:** `cargo test -p paneflow-app detect_file_paths_bounds_canonicalize_probes` fails if first Cmd-hover of a dense 200-column path line performs more than a small bounded number of FS probes.
**Found by:** Performance and resilience
**Lead check:** quote present at `src-app/src/terminal/element/hyperlink.rs:298`; claim confirmed

### 13. [S2] Matching worktree identity restore is untested
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/workspace/worktree.rs:381` in `managed_worktree_from_persisted_record`
```
    let identity = match identity_raw {
        Some(raw) => {
            let expected = WorktreeIdentity::from_persisted(raw)?;
            let actual = WorktreeIdentity::from_path(&path).ok()?;
            if actual != expected {
                log::warn!(
                    "managed worktree: directory identity changed for {}",
                    path.display()
                );
                return None;
            }
```
**What is wrong:** After a current session save, `directory_identity` is `Some`. Restart restore therefore always takes this `Some(raw)` arm. Every live-restore test still passes `directory_identity: None`. There is no test that a matching identity restores, that a mismatched identity drops a marked live record, or that the journal writer emits `directory_identity`.
**Why it matters:** A regression that always returns None in the Some arm would drop every managed worktree on restart and still pass the suite.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Add tests that (1) rehydrate with a matching `directory_identity` succeeds, (2) a marked checkout with a different identity is dropped, and (3) `persisted_pending_worktree_teardowns` / `managed_worktree_def` keep a `Some` identity on the JSON def.
**Done when:** Changing the Some arm to always return None, or dropping `directory_identity` from the journal writer, fails a test in `cargo test -p paneflow-app managed_worktree`.
**Found by:** Verifiability
**Lead check:** quote present at `src-app/src/workspace/worktree.rs:381`; claim confirmed

### 14. [S2] git run_git honors repo hooksPath and fsmonitor
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/workspace/worktree.rs:600` in `run_git`
```
fn run_git(repo: &Path, args: &[&str], deadline: Duration) -> Result<String, String> {
    let mut cmd = Command::new("git");
    cmd.arg("-C").arg(repo).args(args);
    cmd.env("GIT_TERMINAL_PROMPT", "0");
```
**What is wrong:** Every git probe here (and the twin in `src-app/src/diff/git.rs:158-163`) sets only `GIT_TERMINAL_PROMPT=0`. There is no `GIT_CONFIG_NOSYSTEM`, no `env_remove` of `GIT_DIR`/`GIT_WORK_TREE`/`GIT_SSH_COMMAND`, and no `git -c core.hooksPath=/dev/null` / `core.fsmonitor=` prelude. `git status`, `git worktree add`, and `git diff` therefore honor the opened repo's `.git/config`.
**Why it matters:** A repo delivered as a tarball can set `core.hooksPath` or `core.fsmonitor` to a script inside the tree. Opening Review or creating a Launch Pad worktree executes that script as the PaneFlow user, outside the agent pane.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Centralize git spawn behind one helper that always passes `-c core.fsmonitor=` `-c core.hooksPath=/dev/null` `-c diff.external=` and `env_remove("GIT_DIR")`, `env_remove("GIT_WORK_TREE")`, `env_remove("GIT_SSH_COMMAND")`, plus `GIT_CONFIG_NOSYSTEM=1`. Use that helper from `worktree.rs` and `diff/git.rs`.
**Done when:** A fixture repo whose `.git/config` sets `core.fsmonitor` and `core.hooksPath` to a marker script can be listed, diffed, and worktree-added without the marker running; `cargo test -p paneflow-app git_run_disables_repo_hooks` fails if any production git `Command` in those two modules lacks the prelude.
**Found by:** Security
**Lead check:** quote present at `src-app/src/workspace/worktree.rs:600`; claim confirmed

### 15. [S2] Two porcelain parsers disagree on valid worktrees
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/workspace/worktree.rs:629` in `parse_worktree_porcelain`
```
pub fn parse_worktree_porcelain(stdout: &str) -> Vec<WorktreeEntry> {
    let mut entries = Vec::new();
    let mut path: Option<PathBuf> = None;
    let mut branch: Option<String> = None;
    for line in stdout.lines().chain(std::iter::once("")) {
        if line.is_empty() {
            if let Some(p) = path.take() {
                entries.push(WorktreeEntry {
```
**What is wrong:** Launch Pad / managed teardown parse `git worktree list --porcelain` here (a `worktree ` line is enough). The Review/diff stack reimplements the same porcelain in `src-app/src/diff/git.rs:101` (`parse_worktrees_from_str`) and only keeps an entry when both `worktree` and `HEAD` are present.
**Why it matters:** Launch Pad collision checks and the Worktree-scope picker can see different checkout sets for the same repo. A HEAD-less entry is listed in one path and omitted in the other.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Keep one porcelain parser in `workspace/worktree.rs`. Have `diff::git` map that result into its richer `Worktree` (sha/bare/main) instead of parsing again.
**Done when:** `rg "worktree list --porcelain" -g '*.rs'` has a single parser function; `cargo test -p paneflow-app parse_worktree_porcelain` and the diff-side listing agree on fixtures that include detached/HEAD-less entries.
**Found by:** Design and maintainability
**Lead check:** quote present at `src-app/src/workspace/worktree.rs:629`; claim confirmed

### 16. [S2] Worktree CWD scan has no wall-clock deadline
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/workspace/worktree.rs:1051` in `worktree_has_live_process_cwd`
```
    let pids = pids_by_type(ProcFilter::All)
        .map_err(|error| format!("cannot enumerate processes before worktree removal: {error}"))?;
    if pids.len() > MAX_PROCESS_CWD_SCAN {
        return Err(format!(
            "process CWD scan exceeded {MAX_PROCESS_CWD_SCAN} processes"
        ));
```
**What is wrong:** Teardown scans every process (cap 65_536) with `getsid`, `pidinfo`, then `process_cwd` + `canonicalize` for same-UID / descendant candidates. Git calls in this module have `GIT_DEADLINE`; this scan has only a count cap, no wall-clock timeout.
**Why it matters:** Closing a managed worktree on a busy machine can occupy a blocking-pool thread for seconds. Other `smol::unblock` work queues behind it; teardown also fails closed and leaves the checkout.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Bound the scan with a deadline (reuse `GIT_DEADLINE`). Skip remaining best-effort PIDs when the budget expires, and fail only if a required (descendant / protected-session) probe could not complete in time.
**Done when:** `cargo test -p paneflow-app worktree_has_live_process_cwd_respects_deadline` fails if the scan has no deadline argument or constant.
**Found by:** Performance and resilience
**Lead check:** quote present at `src-app/src/workspace/worktree.rs:1051`; claim confirmed

### 17. [S2] copy_env_files follows .env symlinks out of the repo
**State:** PENDING
**Severity:** S2 (major)
**Where:** `src-app/src/workspace/worktree.rs:1238` in `copy_env_files`
```
        if !name_s.starts_with(".env") {
            continue;
        }
        if !entry.path().is_file() {
            continue;
        }
        let dst = dst_root.join(&name);
        if dst.exists() {
            continue;
        }
        let src = entry.path();
        match std::fs::copy(&src, &dst) {
```
**What is wrong:** `Path::is_file` and `std::fs::copy` both follow symlinks. A top-level `.env*` name that is a symlink to a file outside the repo is treated as a regular env file and its target bytes are copied into the new worktree. Launch Pad always calls this; `paneflow up` defaults `copy_env` to true. The same module already refuses to follow symlinks when reading diff working-tree text.
**Why it matters:** A hostile repo can plant `.env` -> `~/.ssh/id_rsa`. Creating a managed worktree materializes that secret as a regular file under `<repo>.worktrees/`, where the launched agent can read it.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Use `symlink_metadata` and skip anything that is not a regular file. Copy only if the destination is created as a new regular file (`O_EXCL`) so a planted dest symlink cannot be followed either.
**Done when:** `cargo test -p paneflow-app copy_env_files_skips_symlinks` fails if `src/.env` as a symlink to a file outside the tempdir produces a dest `.env`.
**Found by:** Security
**Lead check:** quote present at `src-app/src/workspace/worktree.rs:1238`; claim confirmed

### 18. [S3] sparkle-dist.sh curl has no timeout
**State:** PENDING
**Severity:** S3 (minor)
**Where:** `scripts/sparkle-dist.sh:48` in `n/a`
```
    echo "Fetching Sparkle ${SPARKLE_VERSION} from ${SPARKLE_URL}" >&2
    curl --fail --location --silent --show-error "$SPARKLE_URL" --output "$download"
```
**What is wrong:** The Sparkle tarball fetch has no `--max-time` / `--connect-timeout` and no retry budget. A stalled GitHub connection hangs `curl` until the runner or operator kills it.
**Why it matters:** Release and local bundle packaging block indefinitely on a hung download; `set -euo pipefail` never fires because `curl` does not exit.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Add `curl --connect-timeout 15 --max-time 120 --retry 3 --retry-all-errors` so a dead fetch fails the script instead of hanging.
**Done when:** `grep -n 'max-time' scripts/sparkle-dist.sh` prints a curl invocation with `--max-time` (or `--connect-timeout` plus `--max-time`) on the Sparkle download line.
**Found by:** Performance and resilience
**Lead check:** quote present at `scripts/sparkle-dist.sh:48`; claim confirmed

### 19. [S3] files_hydration apply predicate only tests the false case
**State:** PENDING
**Severity:** S3 (minor)
**Where:** `src-app/src/app/files_sidebar/watch.rs:241` in `tests::files_hydration_ignores_stale_generation`
```
        if should_apply_files_hydration(true, root, root, current_generation, stale_generation) {
            files_tree_generation = stale_generation;
        }
        if should_apply_files_hydration(true, root, root, current_generation, stale_generation) {
            files_event_rx_generation = Some(stale_generation);
        }
```
**What is wrong:** The only unit coverage of `should_apply_files_hydration` asserts the stale-generation false case. It never asserts that matching generation+root+open sidebar returns true, or that a closed sidebar / root mismatch returns false. An always-false predicate still passes this test.
**Why it matters:** A hydration apply predicate that never returns true would leave the Files sidebar on the empty root shell after every open, and `cargo test` would still be green.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Assert the truth table directly: apply when open+same root+same generation; refuse when sidebar closed, root differs, or generation differs.
**Done when:** Making `should_apply_files_hydration` always return false fails `cargo test -p paneflow-app files_hydration`.
**Found by:** Verifiability
**Lead check:** quote present at `src-app/src/app/files_sidebar/watch.rs:241`; claim confirmed

### 20. [S3] worktree_checkout_may_exist git-error arm is untested
**State:** PENDING
**Severity:** S3 (minor)
**Where:** `src-app/src/app/launch_pad.rs:77` in `worktree_checkout_may_exist`
```
    match worktree::list_worktrees(repo_root) {
        Ok(entries) => entries.into_iter().any(|entry| entry.path == path),
        // Losing lifecycle evidence is worse than retaining a reservation
        // when git cannot prove the failed creation left nothing behind.
        Err(_) => true,
    }
```
**What is wrong:** The fail-closed `Err(_) => true` arm is untested. `failed_add_with_registered_markerless_checkout_keeps_reservation` only checks a path that already exists (the `Ok(true)` early return).
**Why it matters:** Changing the git-error arm to false would drop the in-flight ownership reservation when `git worktree list` fails, with no failing test.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Drive `worktree_checkout_may_exist` with a missing path and a non-repo `repo_root` so `list_worktrees` errors, and assert true. Also assert false for a missing path in a real repo with an empty worktree list.
**Done when:** Changing `Err(_) => true` to `false` fails a test in `cargo test -p paneflow-app worktree_checkout_may_exist`.
**Found by:** Verifiability
**Lead check:** quote present at `src-app/src/app/launch_pad.rs:77`; claim confirmed

### 21. [S3] Launch Pad git fixture tests return on init failure
**State:** PENDING
**Severity:** S3 (minor)
**Where:** `src-app/src/app/launch_pad.rs:922` in `tests::launch_pad_plan_uses_hashed_path_when_slug_path_is_claimed`
```
        if !test_git(&repo_root, &["init"]) {
            return;
        }
```
**What is wrong:** The only behavioral test of `launch_pad_worktree_plan` returns without asserting if git init fails, and again if `git worktree add` fails. A green run does not mean the hashed-path collision rule was exercised.
**Why it matters:** CI or a host without a working git fixture reports the Launch Pad collision/reservation tests as passed while the collision-resolved path never ran.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Replace the early `return`s with `assert!(test_git(...), "git fixture required")`. Keep a later worktree-add failure as an assertion, not a skip.
**Done when:** Forcing `test_git` to fail makes `launch_pad_plan_uses_hashed_path_when_slug_path_is_claimed` fail instead of pass.
**Found by:** Verifiability
**Lead check:** quote present at `src-app/src/app/launch_pad.rs:922`; claim confirmed

### 22. [S3] Sparkle scheduled-reminder YES is untested
**State:** PENDING
**Severity:** S3 (minor)
**Where:** `src-app/src/sparkle.rs:198` in `handles_scheduled_reminders`
```
extern "C" fn handles_scheduled_reminders(_this: &Object, _command: Sel) -> BOOL {
    YES
}
```
**What is wrong:** Silent Sparkle policy is that PaneFlow owns scheduled reminders (`YES`) and then shows none. `delegate_policy_never_installs_immediately_or_relaunches` exercises `hold_update_until_quit`, `prevent_relaunch`, and `suppress_scheduled_update_ui`, but never this callback.
**Why it matters:** Flipping the return to `NO` hands scheduled-update UI back to Sparkle and would not fail the policy test.
**How to observe it:** Static finding; not executed.
**Recommended fix:** In `delegate_policy_never_installs_immediately_or_relaunches`, assert `handles_scheduled_reminders(...) == YES`.
**Done when:** Changing `handles_scheduled_reminders` to return `NO` fails `cargo test -p paneflow-app delegate_policy_never_installs_immediately_or_relaunches`.
**Found by:** Verifiability
**Lead check:** quote present at `src-app/src/sparkle.rs:198`; claim confirmed

### 23. [S3] Conflicting worktree identity Keep arm is untested
**State:** PENDING
**Severity:** S3 (minor)
**Where:** `src-app/src/workspace/worktree.rs:143` in `merge_managed_worktree_records`
```
        match (&previous.identity, &worktree.identity) {
            (Some(left), Some(right)) if left != right => {
                log::warn!(
                    "managed worktree: conflicting directory identity for {}; keeping checkout",
                    previous.path.display()
                );
                previous.teardown = TeardownPolicy::Keep;
```
**What is wrong:** The documented fail-closed rule for conflicting directory identities has no test. `same_workspace_duplicate_records_preserve_keep_policy` only merges Auto+Keep with `identity: None`.
**Why it matters:** Removing the identity-mismatch Keep arm leaves Auto teardown in place for duplicate session records that disagree about which directory sits at the path, so restart can delete the wrong checkout.
**How to observe it:** Static finding; not executed.
**Recommended fix:** Unit-test `merge_managed_worktree_records` with two same-path records whose identity (and separately `repo_root`/`branch`) differ, asserting teardown becomes Keep.
**Done when:** Deleting the identity-mismatch Keep assignment fails `cargo test -p paneflow-app merge_managed_worktree_records`.
**Found by:** Verifiability
**Lead check:** quote present at `src-app/src/workspace/worktree.rs:143`; claim confirmed

## Ignored (intentional)
- **Regex and OSC-8 hits with ssh/git/ftp schemes are marked is_openable** at `src-app/src/terminal/element/hyperlink.rs:121`: `all schemes captured by \`URL_REGEX_PATTERN\` are considered openable, since \`open::that\` ultimately defers to the OS handler` (`src-app/src/terminal/element/hyperlink.rs:114-117`)
- **Flow tick issues sequential blocking surface.read calls** at `src-app/src/cli/flow_cmd.rs:505`: `Scheduling is a single-threaded tick loop (no threads, no async)` (`src-app/src/cli/flow_cmd.rs:13-14`)
